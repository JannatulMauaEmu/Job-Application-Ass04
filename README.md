getElementById is used for returning a single element but getElementbyClassName returns all the elements thats are matched with names.querySelector is used for finding first matching element but querySelectorAll is used for finding all the elements.
for creating and inserting used document.createElement('tagname')

